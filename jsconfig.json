// Install the @types/prop-types package for type definitions
// Run the command "npm install @types/prop-types"
// Or use "yarn add @types/prop-types" if you're using Yarn
"scripts": {
  "install-prop-types": "npm install @types/prop-types"
},
"compilerOptions": {
  "jsx": "react",
  "paths": {
    "@/*": ["./*"]
  }
}
  "compilerOptions": {
    "jsx": "react",
    "paths": {
      "@/*": ["./*"]
    }
  }
}
